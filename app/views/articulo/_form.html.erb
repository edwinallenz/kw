<%= simple_form_for @articulo, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :codigo %>
  <%= f.input :nombre %>
  <%= f.input :tecnica_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                articulo_path, :class => 'btn' %>
  </div>
<% end %>
